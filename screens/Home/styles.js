import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  loading: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  loadingContainer: {
    marginHorizontal: 15,
    marginBottom: 5,
  },
});

export default styles;
